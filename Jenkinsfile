#!/usr/bin/env groovy
//Leave the above line alone. It identifies this as a groovy script

//Define variables
def utfPaths = ["CI-Test.lvproj"]
def lvVersion = "14.0"

//Define pipeline script used. This pipeline script lives in a global pipeline library
CITestPipeline(utfPaths, lvVersion)